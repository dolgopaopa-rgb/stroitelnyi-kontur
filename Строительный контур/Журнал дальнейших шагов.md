# Журнал дальнейших шагов

## 2026-06-03 - создана рабочая папка программы

**Запрос.** Сделать отдельную папку на GitHub с названием программы, чтобы можно было работать с другого компьютера, и дальше сохранять каждый шаг там.

**Что сделано.**

- Создана папка `Строительный контур`.
- Добавлен файл `README.md` с кратким описанием, где смотреть контекст и куда записывать новые шаги.
- Добавлен этот журнал дальнейших шагов.

**Как вести дальше.**

- После каждой доработки добавлять новую запись в этот файл.
- Писать человеческим языком: что изменили в программе, кому это важно, что проверить.
- Технические детали оставлять в `docs/16-project-worklog.md`, если они нужны.

**Текущая точка.**

- Последняя доработка программы: в разделе `Сметы` добавлен возврат задания менеджеру с причиной и просмотр фото каруселью.
- Изменения выложены на постоянный сайт.
- В MAX отправлено короткое пользовательское сообщение без технической информации.

## 2026-06-03 - восстановлена передача замечаний из MAX в обратную связь

**Запрос.** Люди пишут замечания в рабочий чат MAX, но они не появляются в разделе `Обратная связь` в Контуре.

**Что нашли.**

- MAX-бот сообщения из чата видел.
- При отправке в Контур бот получал отказ авторизации.
- Причина: после обновления серверных доступов у приложения бот остался со старыми данными для входа в Контур.
- Сообщения не пропали: они сохранялись в локальном журнале бота на сервере.

**Что сделано.**

- Проверена авторизация бота к Контуру.
- Серверные доступы бота синхронизированы с текущими доступами приложения без публикации паролей в репозитории или чате.
- Исправлен формат файла настроек бота на сервере.
- Бот перезапущен.
- После контрольной проверки оставлен один рабочий процесс бота, чтобы сообщения не обрабатывались дважды.
- Накопленные ошибочные сообщения из журнала бота повторно отправлены в Контур.

**Результат.**

- Найдено 40 записей с ошибкой отправки.
- 37 уникальных сообщений успешно досланы в `Обратную связь`.
- 3 записи пропущены как дубли, пустые или служебные.
- API Контура подтвердил наличие 83 записей обратной связи; свежая запись есть за 03.06.2026.

**Что проверить.**

- Открыть раздел `Обратная связь` и убедиться, что последние сообщения из MAX появились.
- Отправить новое короткое сообщение в чат MAX и проверить, что оно появляется в Контуре без ручной досылки.

**Дальше желательно.**

- Добавить отдельный контроль состояния MAX-бота: если бот видит сообщения, но Контур снова не принимает их, система должна показывать предупреждение руководителю.

## 2026-06-03 - проверены замечания, отправленные в работу, и подсвечены кнопки статуса

**Запрос.** Проверить, что отправленные в работу замечания действительно поменяли статус. Сделать так, чтобы при нажатии `В работу` было визуально понятно, что кнопка сработала.

**Что проверено.**

- В базе Контура сейчас 83 сообщения обратной связи.
- 11 сообщений имеют статус `В работе`.
- Последние изменения статуса были внесены 03.06.2026 около 09:22-09:23.

**Что сделано.**

- В разделе `Обратная связь` кнопка после нажатия сразу показывает состояние `Отправляю...`.
- После успешного изменения статуса кнопка `В работу` становится активной и подсвеченной.
- Для статуса `Готово` добавлена отдельная зеленая подсветка.
- Если сервер не примет изменение, кнопка вернется в прежнее состояние и покажет сообщение об ошибке.
- Версия PWA-кеша обновлена, чтобы у коллег подтянулся новый интерфейс.

**Что проверить.**

- Открыть `Обратная связь`.
- На новом сообщении нажать `В работу`.
- Убедиться, что кнопка сначала показывает отправку, а потом превращается в подсвеченную `В работе`.

## 2026-06-03 - исправлен вход с мобильных устройств

**Запрос.** Коллеги не могут зайти в программу с телефонов.

**Что нашли.**

- Вход был завязан на системное окно авторизации браузера.
- На мобильных устройствах такой вход работает нестабильно: окно может не сохранять пароль, конфликтовать с PWA и непонятно выглядеть для сотрудников.

**Что сделано.**

- Добавлена обычная страница входа `/login` внутри программы.
- На странице входа есть поля `Логин` и `Пароль`.
- У пароля есть кнопка `Показать` / `Скрыть`.
- Логины и пароли сотрудников остались прежними.
- После успешного входа программа сохраняет сессию, чтобы мобильный браузер не просил пароль на каждом шаге.
- Если сессия истекла, программа сама отправляет пользователя на страницу входа.
- Выход из программы теперь ведет на страницу входа, а не в системное окно браузера.
- Service worker обновлен так, чтобы страница входа не кэшировалась как главная страница программы.
- Старый способ входа через Basic Auth оставлен только для служебных проверок и обратной совместимости.

**Что проверить.**

- Открыть `https://kontur.derevgroup.ru` на телефоне.
- Убедиться, что открылась нормальная форма входа Контура.
- Войти под своим логином и паролем.
- Проверить, что пароль можно показать перед входом.
- Закрыть браузер или переключиться между приложениями и проверить, что вход не слетает сразу.

## 2026-06-03 - доработана вставка пароля с телефона

**Запрос.** С мобильного нельзя нормально вставить скопированный пароль, а если ошибся, пароль сложно увидеть и проверить.

**Что нашли.**

- Кнопка показа пароля находилась внутри поля и могла мешать долгому нажатию, выделению и вставке на телефоне.
- На мобильных браузерах поле пароля должно быть максимально простым: отдельное поле, отдельные кнопки действий ниже.

**Что сделано.**

- Поле пароля сделано полноценной ширины, без перекрывающей кнопки справа.
- Добавлены отдельные кнопки `Показать пароль`, `Вставить пароль` и `Очистить`.
- Если браузер разрешит доступ к буферу обмена, кнопка `Вставить пароль` сама вставит скопированный пароль.
- Если телефон не разрешит автоматическую вставку, программа покажет подсказку: удержать палец в поле пароля и выбрать `Вставить`.
- Лишние пробелы и переносы строк вокруг пароля убираются при входе, чтобы случайное копирование не ломало авторизацию.
- Поле логина тоже защищено от автозамены и автоматических заглавных букв на телефоне.
- Версия мобильного кэша обновлена, чтобы телефоны получили новую форму входа.

**Что проверить.**

- Открыть `https://kontur.derevgroup.ru/login` с телефона.
- Вставить пароль через кнопку `Вставить пароль`.
- Нажать `Показать пароль` и проверить, что пароль виден.
- Очистить поле кнопкой `Очистить` и попробовать вставить пароль обычным долгим нажатием в поле.

**Результат деплоя.**

- Изменения выложены на `https://kontur.derevgroup.ru/login`.
- Проверено: страница входа открывается, кнопки пароля видны, авторизация проходит, приложение открывается.
