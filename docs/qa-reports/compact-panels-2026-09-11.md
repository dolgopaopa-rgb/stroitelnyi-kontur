# Компактные списки и выравнивание: 11 сентября 2026

## Выпуск

Код: `fc700876af1a21aa68bdabcbd4c54c2c944b4839`, ветка `codex/dashboard-compact-20260911`.
Тестовая и основная версии опубликованы. Основной выпуск: 2026-09-11 06:08:17 UTC, тестовый: 05:03:44 UTC. Публичный `/version` в обоих окружениях подтверждает `fc70087`. Проверка опубликованного интерфейса завершена, итоговое сообщение отправлено в существующий чат коллег MAX.

## Изменения

- «Сегодня»: задачи, материалы и комментарии показывают две записи, остальные раскрываются на месте. Соседние панели сохраняют одинаковую высоту.
- «Сигналы»: объекты, задачи и уведомления показывают две записи с раскрытием; панели и их шапки выровнены. Верхние показатели распределены по доступной ширине, ручки перестановки не закрывают значения.
- «Объекты»: переключатели выше, компактнее и разделены промежутками. Узкая колонка при открытом объекте использует отдельную адаптивную раскладку: статусы, сумма и прораб больше не пересекаются.
- «Задачи»: два компактных объекта с раскрытием остальных, промежуток между названием и индикаторами. Смена объекта в верхнем фильтре обновляет список задач и раскрывает выбранный скрытый объект, сохраняя возможность ручного сворачивания.
- «Локации»: две записи с раскрытием, увеличен промежуток под названием. Общие карточки получили разделение заголовка и подробностей; статус активного объекта не сжимается побуквенно.
- Версия статических ресурсов и service worker: `20260911-compact-panels`. Совместимый JS пересобран.

Сервер, контракты API, роли, авторизация, интеграции, схема базы и рабочие документы не изменены.

## Проверки

| Проверка | Подтверждённый результат |
|---|---|
| Новые регрессионные тесты | 20/20 PASS на финальном коде: раскрытие, сохранение состояния, фильтры, выбор третьего объекта, геометрия кнопок и статусов |
| Файловые сценарии Анны | 40 Python-проверок PASS; финальный повтор 24/24 браузерных сценариев PASS: загрузка, скачивание, замена, дубликаты, повтор после ошибки, PDF, изображения, галерея, ограничения аудитора |
| Общий QA Елены | 16 встроенных проверяющих модулей: 144 OK, 0 FAIL, 1 WARN. WARN: внешний просмотр без cookie не подтверждён |
| Навигация Нины | 188/188 переходов, 11 ролевых вариантов на desktop/mobile; 0 ошибок браузера, шрифты подтверждены через CDP |
| Существующие браузерные тесты | 60 PASS, 4 предусмотренных тестами пропуска, 0 FAIL; прогон пересёк последнюю правку только колонок объектов |
| Зина | Финальные 15/15 проверок выбранного объекта PASS; прежние смещения шапок и пересечения текста устранены |
| Рада | 158 ширин, 320-1920 с шагом 16 и точками перелома, дополнительно 2560/3440/3840. 1880 исходных замеров; после последнего изменения отдельно 386 замеров выбранного объекта, 22/22 прокруток и доступов к последней вкладке. 371 PNG декодированы и непустые |
| Ольга | Проверка кода, экранирования, границ ролей и формата MAX: новых блокирующих замечаний нет |
| Тестовый сайт | 7 реальных учётных ролей, 90 состояний на 390/1440/1920; 0 ошибок ответов/JS, 0 выходов страницы по горизонтали |
| Основной сайт после выпуска | 7 реальных учётных ролей, 84 состояния на 390/1440/1920; 0 ошибок ответов/JS, 0 горизонтальных выходов, 0 повреждённых видимых изображений по диагностике |

Общий QA и часть ширин выполнены на предпоследнем CSS `08a237e6...`. Последняя правка ограничена строками выбранного объекта. Её закрывают независимые финальные проверки Рады, Зины и 20 регрессионных тестов на CSS `4e5f7513...`. Стабильность остальных файлов подтверждена. Дополнительный повтор старых object-тестов после остановки агента не принят как доказательство: порт локального сервера был занят.

## Ограничения

- Это Chrome/Windows с эмуляцией размеров, не физические iPhone/iPad и не Safari.
- При 320px отдельные внутренние карточки имеют ширину 270-274px; в узкой сводке выбранного объекта длинное имя прораба переносится в четыре строки. Пересечений и обрезки не выявлено; это небольшие оставшиеся замечания плотности, не блокирующие операции.
- Старые облачные вложения на тестовом сервере возвращают 502: там локальное хранилище и нет доступа к рабочему облаку. Это существовавшее до выпуска ограничение; рабочие секреты на тестовый сервер не копировались.
- До выпуска на основном сервере PDF, JPEG и сметные файлы скачивались с HTTP 200 под семью ролями. Загрузка проверялась на изолированных данных, не созданием тестовых записей у коллег.
- Два диагностических счётчика brokenImages в тестовой роли руководителя строительства не подтверждены как видимые поломки: на снимках этих состояний повреждённого изображения не видно. Они не объявляются устранёнными.
- Проверка перехода ранее установленной PWA со старого кэша не завершена: были ERR_SSL_PROTOCOL_ERROR, timeout регистрации и прерывание контекста штатной автоперезагрузкой. Это не PASS. Версионирование ресурсов проверяется отдельно по опубликованным файлам.
- Внешний просмотр без cookie остаётся непроверенным. Результаты не означают исчерпывающую проверку всех данных и устройств.

## Сохранность

До выпуска создана согласованная SQLite-копия и ZIP локальных загрузок в каждом окружении: `data/backups/production-layout-20260911`. Сохранены прежние образы и параметры контейнеров для отката. Меняется только контейнер приложения; Caddy и другие сервисы не перезапускаются.

После обоих выпусков: integrity=ok, схема и настройки совпадают, изменения числа записей отсутствуют. На основном сервере проверены все 56 исходных локальных файлов: отсутствующих и изменённых нет. Повторные 18 скачиваний PDF/JPEG/сметных файлов под семью ролями успешны, SHA256 содержимого совпадает с проверкой до обновления.

## Доказательства

Локальные результаты текущего задания сохранены в `.tmp/` этой рабочей копии, не смешаны с прежними `qa-artifacts/latest`:

- `maria/report/results.json`, `anna/coordinator-final-fc70087/results.json`;
- `elena/20260911-final-qa/report.md`;
- `rada/independent-20260911/report.md`, `selected-recheck/results.json`;
- `zina/2026-09-11T05-02-44-227Z/report.md`;
- `nina/report/results.json`, `nina/role-navigation/results.json`;
- `live-layout-staging-fc70087/report.json`, `staging-smoke.json`, `production-smoke-before.json`;
- `staging-checkpoint-after.json`, `pwa-layout-result.json`.
- `production-smoke-after.json`, `production-checkpoint-after.json`, `live-layout-production-fc70087/report.json`.

## Итог Публикации И MAX

Обновление завершено в обоих окружениях. Основной сайт: https://kontur.derevgroup.ru/today?build=fc70087. Тестовый: https://staging.79-143-30-43.sslip.io/today?build=fc70087.

MAX: сообщение отправлено один раз штатным отправителем в подтверждённый существующий чат обратной связи коллег. Команда завершилась с exit 0 и ответом `sent`; это подтверждение успешной отправки, не прочтения коллегами. Текст содержит выполненные изменения, проверенные сценарии и ссылку на рабочий сайт. Копия: `.tmp/olga/max-final.md`, результат: `.tmp/max-send-result.txt`. Токены не выводились и не копировались.

Семь независимых участников проверки: Мария, Анна, Елена, Рада, Зина, Ольга, Нина. Нина завершила основной тестовый процесс, но её агент остановился по лимиту использования до итогового текста; координатор проверил сохранённые результаты и не приписывает ей незавершённый повтор. Все запущенные координатором процессы завершены, активные агенты закрыты.

Первое получение меню технадзора в production-проверке произошло раньше окончания инициализации (пустой список навигации). Повтор после ожидания скрытия загрузочного экрана проверил 15 состояний этой роли без ошибок; изменение сделано только в QA-помощнике, не в приложении.
